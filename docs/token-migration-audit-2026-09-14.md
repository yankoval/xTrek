# Сверка перед миграцией — 14.09.2026

Объём: пункты 1–2 плана. Production проверен только чтением; новых токенов и подписей не запрашивали.

## Git и изоляция

Выполнен fetch `origin` репозитория `https://github.com/yankoval/xTrek.git`. Актуальный main — `3ec79a7` (merge #90). Он содержит `3781210`, однократный мастер по расписанию, и `06971b7` (#87), явный выбор JWT для True API.

Создана новая ветка **`feature/token-purpose-model`** от `origin/main`, отдельное рабочее дерево `/Users/ivankiselev/Documents/CodexCHETHE/xtrek-token-purpose`. Работа не ведётся в старой экспериментальной ветке.

`xtrek-sscc-auth` теперь чистый и отслеживает `origin/feature/scheduled-token-worker`: прежние изменения уже вошли в историю репозитория. Старое дерево `xtrek-true-api-uuid` сохраняет untracked документы и инструменты эксперимента. Они не переносились с удалением и не перезаписывались. Применимых файлов AGENTS.md внутри нового/исходных деревьев не обнаружено; действуют общие инструкции пользователя из задачи.

## Реальный production

Соединение `yc-worker`, hostname `compute-vm-2-2-20-hdd-1778346506147`.

- Celery: active, MainPID751, рабочий релиз `/home/ivankiselev1975/Scripts/Testing/releases/xTrek-main-06971b7`.
- Мастер: `/home/ivankiselev1975/Scripts/Testing/releases/xTrek-token-scheduled-20260910`.
- Таймер мастера active; в момент проверки LastTrigger — 14.09.2026 11:10:00 МСК.

Контрольные суммы файлов вычислены на сервере и сопоставлены с содержимым Git, а не только с именами каталогов:

| Файл | Celery | Отдельный мастер |
|---|---|---|
| `xtrek/tokens.py` | совпадает с `06971b7` / новым main | совпадает со старой базой `1a583f4` |
| `xtrek/token_worker.py` | старая версия из `06971b7` | совпадает с новым main `3ec79a7` |
| `xtrek/crpt_auth.py` | совпадает с новым main | совпадает с новым main |
| `xtrek/utils.py` | совпадает с `06971b7` / новым main | совпадает с `1a583f4` |
| `xtrek/tasks.py` | совпадает с `06971b7` / новым main | совпадает с `1a583f4` |

Следовательно, новый main объединяет текущие исправления Celery и фактический код планировочного мастера. При будущей поставке нельзя считать всё отдельное дерево мастера равным main: вспомогательные файлы в нём старее. Эта проверка не является аудитом всех файлов/библиотек, конфигурации или текущих очередей.

## Результат пункта 2

Добавлен модуль `xtrek/token_registry.py` с моделью назначения, контура, ИНН, формата и происхождения срока; явными СУЗ-идентификаторами; выбором по назначению; отдельным версионированным локальным хранилищем и явным адаптером legacy. Подробности — `token-registry.md`.

Пройдены 16 локальных тестов. Текущие рабочие потребители не подключены к новой модели — это пункт 3, который в этом запросе не выполняется. Существующая политика «JWT для True API» остаётся в старых потребителях до следующего этапа, где будет заменена на выбор назначения.

Ничего не опубликовано и не развёрнуто: без push/PR/merge, без изменения production-токенов, файлов конфигурации, подписанта, служб и расписаний.
